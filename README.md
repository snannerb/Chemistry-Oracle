# Chemistry Oracle

## About
This code was generated by [CodeCraftAI](https://codecraft.name)

**User requests:**
build a web app that generates a random number from 0 to 100 to determine the love chemistry between two people. Include a prompt to enter names. Include a measure bar that leaves an explanation for every 10% increase. Use a red and grey user interface. Use a pixel font.

Check OUTPUT.md for the complete unaltered output.

## Project Plan
```
Here’s a **simple and clear project plan** for building the web app based on the requirements:

---

## **Project Plan: Love Chemistry Web App**

### **Project Overview**
- **Objective**: Develop a web app that generates a random "love chemistry" percentage between two people, displays the result with a measure bar, and provides explanations based on the percentage range.
- **Platform**: Web (HTML, CSS, JavaScript).

---

### **Main Tasks**

#### **1. Set Up Project Structure**
   - Create a project folder with the following files:
     - `index.html` for the app structure.
     - `style.css` for styling.
     - `script.js` for functionality.

#### **2. Build the HTML Structure**
   - Input Section:
     - Two text fields for entering names.
     - A button to generate the result.
   - Output Section:
     - A container to display the percentage.
     - A measure bar (e.g., progress bar) to visually represent the percentage.
     - A text area for the percentage explanation.

#### **3. Implement JavaScript Functionality**
   - Random Number Generator:
     - Generate a random number between 0 and 100.
   - Result Display:
     - Update the percentage display, measure bar, and explanation dynamically based on the result.

#### **4. Style the App (CSS)**
   - Use a **red and grey** color scheme:
     - Red for buttons, highlights, and accents.
     - Grey for backgrounds and secondary elements.
   - Use a **pixel font** for all text.

#### **5. Add Explanations for Percentage Ranges**
   - Map the result to the predefined percentage range explanations (e.g., 0-10%: "Not a match").

#### **6. Testing and Debugging**
   - Test the app across different browsers and devices.
   - Ensure the UI is responsive and functional.

#### **7. Deployment**
   - Deploy the app to a web hosting platform (e.g., GitHub Pages, Netlify).

---

### **Technical Considerations**

#### **Frontend Technologies**
- **HTML**: Create the structure of the app (input fields, button, output area).
- **CSS**: Style the app with the specified color scheme and pixel font.
- **JavaScript**: Handle the random number generation, result calculation, and dynamic updates.

#### **Styling**
- Use a pixel font from Google Fonts or similar (e.g., "Press Start 2P").
- Ensure the measure bar (progress bar) is visually appealing and responsive.

#### **Responsive Design**
- Design the app to be mobile-friendly and responsive.

#### **Dynamic Content**
- Use JavaScript to dynamically update the percentage, measure bar, and explanation text based on the result.

---

### **Timeline (Example)**

| **Task**                       | **Duration** |
|--------------------------------|--------------|
| Set up project structure        | 0.5 day      |
| Build HTML structure            | 0.5 day      |
| Implement JavaScript logic     | 1 day        |
| Style the app                  | 1 day        |
| Add percentage explanations     | 0.5 day      |
| Testing and debugging           | 1 day        |
| Deployment                      | 0.5 day      |
| **Total**                       | **5 days**   |

---

### **Deliverables**
1. Fully functional web app with input fields, a generate button, and output display.
2. Visually appealing design with the specified color scheme and pixel font.
3. Responsive and user-friendly interface.

---

Let me know if you’d like to adjust the timeline or add more details!
```
